<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=restaurant.name%></title>
  </head>
  <body>
    <%- include('./nav.ejs') %>

    <h1>Home Page</h1>

    <p><%=restaurant.name%></p>
    <p><%=restaurant.address%></p>
    <p><%=restaurant.phone%></p>

    <%if(restaurant.isOpen){%>
    <p>Yes we are open!</p>
    <%}else if(!restaurant.isOpen){%>
    <p>Sorry, we are closed.</p>
    <%}else{%>
    <p>Check the value of isOpen</p>
    <%}%>
  </body>
</html>
